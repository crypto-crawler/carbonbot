missingok
notifempty
rotate 99999
size 64

dateext
dateformat .%Y-%m-%d-%H-%M.json

postrotate
  pid_file="/tmp/carbonbot-pids/ticker/$(basename $1)"
  pid=$(cat "$pid_file")
  kill -HUP "$pid"
endscript

/carbonbot_data/bbo/*/*/*.ticker {}
